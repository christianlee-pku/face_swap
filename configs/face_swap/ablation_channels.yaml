name: lfw-unet-ablation-channels
seed: 42

dataset:
  type: LFWDataset
  root: data/lfw/processed
  split: train
  manifest: data/lfw/manifest.json

model:
  type: UNetFaceSwap
  channels: 32

loss:
  type: FaceSwapLoss

runner:
  type: BaseRunner
