name: lfw-unet-trt-001
seed: 42

export:
  onnx_path: work_dirs/exports/lfw-unet-baseline-001/model.onnx
  engine_path: work_dirs/exports/lfw-unet-baseline-001/model.trt
  precision: fp16  # options: fp32, fp16, int8
  workspace_size: 2048  # in MB
  max_batch_size: 1
  input_shape: [1, 3, 160, 160]
  dynamic_shapes: false
  trtexec_path: trtexec  # path to trtexec binary

runner:
  type: BaseRunner
